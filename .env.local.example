# Copy to .env.local

# Offline mode: Use IndexedDB (no Supabase server needed). Default for quick start.
VITE_USE_OFFLINE_DB=true

# For Supabase: Set VITE_USE_OFFLINE_DB=false, run "supabase start", then:
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=<run "supabase status" to get the anon key>
